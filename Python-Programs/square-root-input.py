num = input('Enter the number : ')
sqr_root = float(num)**0.5

print("square root is :", float(sqr_root))

#Below line is added to avoid python to close immediately after executing
input('Press ENTER to exit')